const Checkbox = () => {
  return (
    <div className="mt-1 flex gap-x-1">
      <input type="checkbox" />
      <span>Mark is based on match context</span>
    </div>
  );
};

export default Checkbox;
